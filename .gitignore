# Dependencies
node_modules/

# Env files (NEVER commit)
.env
.env.local
.env.*.local

# Build outputs
.vercel/
build/
dist/
__pycache__/
*.pyc

# Local development
.DS_Store
*.log
# deploy_checkpoint.json IS committed — it's the public register of every
# deployed market (contract_address per match_id, all on-chain public data).
# Rotated / superseded checkpoints stay ignored so they don't accumulate.
deploy_checkpoint.OLD-*.json
deploy_checkpoint.*.OLD.json
deploy_checkpoint.bak.json

# IDE
.vscode/
.idea/

# OS
Thumbs.db

.claude/
